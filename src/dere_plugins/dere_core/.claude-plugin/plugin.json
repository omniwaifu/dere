{
  "name": "dere-core",
  "version": "0.9.1",
  "description": "Core dere infrastructure: personality tone, emotion awareness (OCC), memory/recall, knowledge graph integration, and environmental context. Always enabled.",
  "mcpServers": {
    "swarm": {
      "command": "uv",
      "args": [
        "run",
        "python",
        "-m",
        "dere_plugins.dere_core.mcp_server.swarm"
      ],
      "env": {
        "DERE_DAEMON_URL": "${DERE_DAEMON_URL:-http://localhost:8787}",
        "DERE_SESSION_ID": "${DERE_SESSION_ID}",
        "DERE_SWARM_ID": "${DERE_SWARM_ID}",
        "DERE_SWARM_AGENT_ID": "${DERE_SWARM_AGENT_ID}",
        "DERE_SWARM_AGENT_NAME": "${DERE_SWARM_AGENT_NAME}"
      }
    },
    "work-queue": {
      "command": "uv",
      "args": [
        "run",
        "python",
        "-m",
        "dere_plugins.dere_core.mcp_server.work_queue"
      ],
      "env": {
        "DERE_DAEMON_URL": "${DERE_DAEMON_URL:-http://localhost:8787}",
        "DERE_SESSION_ID": "${DERE_SESSION_ID}",
        "DERE_SWARM_ID": "${DERE_SWARM_ID}",
        "DERE_SWARM_AGENT_ID": "${DERE_SWARM_AGENT_ID}"
      }
    },
    "knowledge": {
      "command": "uv",
      "args": [
        "run",
        "python",
        "-m",
        "dere_plugins.dere_core.mcp_server.knowledge_graph"
      ],
      "env": {
        "DERE_DAEMON_URL": "${DERE_DAEMON_URL:-http://localhost:8787}"
      }
    }
  }
}
